<!DOCTYPE html>
<html lang="kr">
<head>
	<meta charset="UTF-8">
	<title>Insert title here</title>
	<script src="../../js/jquery-3.6.0.js"></script>
	<script type="text/javascript">
	$(document).ready(function() {
		//1) a링크 클릭 시 prompt로 나이 입력받기
		//2) 입력받은 나이가 15세 이상인 경우에 링크 이동 - 15세 미만이면 링크 이동X
		$("a").click(function(e) {
			var age = prompt("나이를 입력하세요. (숫자만 입력 가능)");
// 			age >= 15 => 기본동작으로 a태그에 설정해뒀으므로 따로 설정하지 않아도 됨
			if(age < 15) {
// 				event.preventDefault();	//event도 document와 같이 내장 객체 이므로 그냥 사용해도 됨
				e.preventDefault();	//but.파라미터로 e를 넣어두고 e.preventDefault()로 사용하는 편이 나음
			}
		});
	});
	</script>
</head>
<body>
	<p>
		<a href="http://www.google.com" target="_blank">네이버</a>로 이동
	</p>
</body>
</html>