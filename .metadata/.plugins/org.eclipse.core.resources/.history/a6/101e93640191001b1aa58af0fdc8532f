<!DOCTYPE html>
<html lang="kr">
<head>
	<meta charset="UTF-8">
	<title>Insert title here</title>
	<script src="../../js/jquery-3.6.0.js"></script>
	<style type="text/css">
	.emphasis {
		color: red;
		font-size: 1.5em;
	}
	</style>
	<script type="text/javascript">
		$(document).ready(function() {
			//p태그에 마우스를 올리면 글자가 빨간색, 1.5배로 커진다.
			//p태그에 마우스를 내리면 원래대로 돌아간다.
			
// 			//방법1) mouseover / mouseout 이벤트 바인딩
// 			$("p").mouseover(function() {
				
// 			});
			
// 			$("p").mouseout(function() {
				
// 			});

// 			//방법2) on 메서드 사용해서 이벤트 바인딩 - 방법1과 차이점은 동적으로 생성되는 객체에도 적용됨
// 			$("p").on("mouseover", function() {
// 				$(this).css("color", "red");
// 				$(this).css("font-size", "1.5em");
// 			});
			
// 			$("p").on("mouseout", function() {
// 				$(this).css("color", "black");
// 				$(this).css("font-size", "1em");
// 			});

// 			//방법3) on 메서드 한번 사용으로 이벤트 바인딩
// 			$("p").on({
// 				mouseover: function() {
// 					this.className = "emphasis";	//.class도 내장객체
// 				}
// 			   ,mouseout: function() {
// 					this.className = "";	//.class도 내장객체
// 				}
// 			});
			
			//방법4) toggle 메서드 이용 - jquery 메서드
			$("p").on("mouseout mouseover", function() {
				$(this).toggleClass("emphasis");
			});
		});
	</script>
</head>
<body>
	<p class="emphasis">마우스를 올려보세요</p>
</body>
</html>